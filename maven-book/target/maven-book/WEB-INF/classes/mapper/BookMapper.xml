<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "http://mybatis.org/dtd/mybatis-3-mapper.dtd" "mybatis-3-mapper.dtd" >
<mapper namespace="com.kgc.exam.mapper.BookMapper">

    <select id="queryBooks" resultType="com.kgc.exam.entity.Book">
        select id,name,author,publish,publishdate,page,price,content from book
    </select>

   <!-- <select id="queryBooksIntoPage" resultMap="">
        select id,name,author,publish,publishdate,page,price,content from book order by id desc limit #{start},#{pageSize}
    </select>-->
</mapper>
